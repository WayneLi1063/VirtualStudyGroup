<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_margin="16dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/ivChatUserImage"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:src="@color/huskyGold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <TextView
        android:id="@+id/tvChatUserName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Song name"
        android:text="USER Name"
        app:layout_constraintLeft_toRightOf="@id/ivChatUserImage"
        app:layout_constraintTop_toTopOf="@id/ivChatUserImage"
        app:layout_constraintBottom_toBottomOf="@id/ivChatUserImage"
        android:layout_marginStart="60dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="20sp"/>


</androidx.constraintlayout.widget.ConstraintLayout>