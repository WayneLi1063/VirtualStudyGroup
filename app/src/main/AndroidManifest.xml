<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.virtualstudygroup">

    <application
        android:name=".VSGApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ExploreActivity"/>
        <activity android:name=".userManagerActivity.UserProfileEditActivity"/>
        <activity android:name=".MyGroupActivity"/>
        <activity android:name=".chatActivity.ChatLogActivity" />
        <activity android:name=".chatActivity.NewMessageActivity" />
        <activity android:name=".userManagerActivity.RegisterActivity" />
        <activity android:name=".userManagerActivity.LoginActivity" />
        <activity android:name=".chatActivity.MessageActivity" />
        <activity android:name=".userManagerActivity.UserProfileActivity" />
        <activity android:name=".userManagerActivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateGroupActivity">

        </activity>
    </application>

</manifest>